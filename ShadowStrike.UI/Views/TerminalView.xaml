<UserControl x:Class="ShadowStrike.UI.Views.TerminalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#0B0E14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#151921" Padding="20">
            <TextBlock Text=">_ TERMINAL" FontSize="24" FontWeight="Bold" Foreground="#00FF88" FontFamily="Consolas"/>
        </Border>

        <!-- Log Output -->
        <ScrollViewer Grid.Row="1" x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto" Margin="10">
            <ItemsControl x:Name="LogItemsControl" Foreground="#00FF88" FontFamily="Consolas" FontSize="14">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,2"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Command Input -->
        <Border Grid.Row="2" Background="#151921" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text=">" Foreground="#00FF88" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="16"/>
                <TextBox x:Name="CmdInput" 
                         Background="Transparent" 
                         Foreground="White" 
                         BorderThickness="0" 
                         FontFamily="Consolas" 
                         FontSize="16"
                         KeyDown="CmdInput_KeyDown"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
